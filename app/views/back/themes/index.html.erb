<div>
  <h1 class="text-xl font-bold">Back::Themes#index</h1>
  <p>Find me in app/views/back/themes/index.html.erb</p>
  <div>
    <ul>
      <% @themes.each do |theme| %>
        <li>
          <%= theme.title %> -
          <%= link_to "Edit", edit_admin_theme_path(theme.slug) %> -
          <%= link_to "Show", theme_path(theme.slug) %> -
          <%= link_to "Destroy", admin_theme_path(theme.slug),
                      data: { "turbo-method": :delete,
                              confirm: "Etes vous sur de vouloir supprimer cet article ?"}
          %>
        </li>
      <% end %>
    </ul>
    <%= link_to "Créer un nouveau thème", new_admin_theme_path %>
  </div>
<div>
